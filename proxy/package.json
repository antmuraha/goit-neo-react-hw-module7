{
  "name": "demo-proxy",
  "version": "1.0.1",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "proxy-dev": "dotenv -e .env -- node ./local/server.js",
    "gcloud-test": "functions-framework --target=proxy --source=."
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "dotenv-cli": "^8.0.0",
    "express": "^5.1.0"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.0"
  }
}
